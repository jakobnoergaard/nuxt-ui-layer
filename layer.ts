import { defineNuxtModule } from '@nuxt/kit'

export default defineNuxtModule({
    meta: {
        name: 'nuxt-ui-layer',
        configKey: 'nuxtUiLayer',
    },
    defaults: {},
    setup(options, nuxt) {
        // Your layer setup logic here
    }
})